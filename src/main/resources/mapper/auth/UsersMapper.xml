<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.felixstudio.automationcontrol.mapper.auth.UsersMapper">
    <select id="listUsersWithDept" resultType="com.felixstudio.automationcontrol.dto.auth.UsersDTO">
        select u.*, coalesce(string_agg(distinct r.role_name,','),'') as role_names
        from users u
                 left join user_departments ud on u.id = ud.user_id
                 left join user_roles ur on u.id = ur.user_id
                 left join roles r on ur.role_id = r.id
        where ud.department_id = #{deptId}
        group by u.id
    </select>

</mapper>